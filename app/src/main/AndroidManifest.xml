<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bandeng.picuga">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_redesign"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_redesign"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu" />
        <activity android:name=".Settings" />
        <activity android:name=".AudioSettings" />
        <activity android:name=".SelectLevel" />
        <activity android:name=".Game" />
        <activity android:name=".LevelCompleteActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".LossActivity" />
        <activity
            android:name=".SplashScreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LanguageSettingsActivity"></activity>
    </application>

</manifest>